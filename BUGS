Parser cannot generate a valid tree, when expressions of a certain format exist.

Input:
	c = (50 + 19) * 8

----------------------------------


Problem with function calls inside function call arguments
e.g: #call(#print(3, 1), a)

-------------------------------------

Parser will throw an exception even if two rules wouldn't conflict
See: std::shared_ptr<ParserRule> Parser::getNextValidRule() method